# Promote terms across post-baseline discoveries over time

**Status: accepted**

A term becomes a cross-source priority when it is found in post-baseline new
URLs from at least two distinct hostnames, even when those discoveries occur in
different scans days apart. The priority is retained permanently while recent
activity is tracked separately; this preserves the difference between a term
that was never independently validated and one that was validated but has gone
quiet.
